# ForceIA - dependencias de runtime
# LLM
openai>=1.30.0

# HTTP / API
httpx>=0.27.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Config / validacao
python-dotenv>=1.0.0
pydantic>=2.6.0
PyJWT>=2.8.0

# Banco (multi-tenant)
supabase>=2.4.0

# Orquestracao de agentes (grafo + checkpoints + retries)
langgraph>=0.2.0
langchain-core>=0.2.0

# Observabilidade
langfuse>=2.0.0

# Integrações (opcional — instale se FORCEIA_COMPOSIO=1)
# composio>=0.7.0
